<p style="color: green"><%= notice %></p>
<%= link_to root_path do %>
  <p>' < '</p>
<% end%>
<h1><%=@group.name%></h1>

  <% if @expenses.count > 0  %>
    <p><%=number_to_currency @group.expenses.where(user: current_user).sum(:amount)%> </p>
    <%else%> <p>There are no expenses in this Group</p>
  <% end %>

<div id="expenses">
  <% @expenses.each do |expense| %>
  <%= link_to expense_detail_path(expense_id: expense.id, group_id: @group.id) do %>
  <div>
  
              <%= expense.name %>
          <%=number_to_currency expense.amount %>
          
    </div>
    <%end%>
     
    
  <% end %>
</div>

<%= link_to "Add a New Transaction", expense_new_path %>
