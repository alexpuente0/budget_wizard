<div class="bg-azure flex justify-between p-5">
  <%= link_to splash_path do %>
    <p class="font-nova text-white text-xl"> ← </p>
  <% end%>
  <h2 class="font-nova text-white text-xl">DETAILS</h2>
    <div></div>
</div>

<p style="color: green"><%= notice %></p>

  <div class="flex justify-center align-center mb-5 py-5 bg-white border border-gray-200">
  
<p><%= image_tag(image_path("#{@group.icon}"), class: "w-20 h-20 rounded-full")%></p>
<p  class="font-nova py-7 px-5 text-coal"><%=@group.name%></p>
  </div>  

  <div class="flex flex-col justify-center align-center mx-10 mt-8 text-center bg-white border border-gray-200">
        <p class="font-nova font-bold text-lg py-5">  <%= @expense.name %> </p>
      <div class="border border-gray-200 py-5">      
        <p class="font-nova text-coal"> Transaction No. <%= @expense.id %> </p>
        <p class="font-nova text-gray-400"> Created at <%= @expense.created_at %> </p>
  </div>
          <div class="flex align-center m-5 justify-between text-center py-3">
        <p class="font-nova text-coal"> TOTAL PAYMENT:</p><p> <%= number_to_currency @expense.amount %> </p>
          </div>
</div>
